.nav {
  position: absolute;
  right: 1rem;
  > ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    gap: 0.5rem;
  }
  a {
    font-size: 0.75rem;
    font-weight: 400;
  }
}
