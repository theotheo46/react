.error-component {
  display: flex;
  justify-content: center;
  flex-direction: column;
  align-items: center;
  &__title {
    color: var(--color-white);
  }

  &__message {
    color: var(--color-white);
    margin-bottom: 2rem;
  }
}
