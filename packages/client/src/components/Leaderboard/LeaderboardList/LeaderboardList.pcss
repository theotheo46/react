.leaderboard-list {
  width: 100%;
  max-width: 600px;
  margin: 0 auto;

  &__header {
    display: flex;
    gap: 1rem;
    margin: 2.25rem 0;
  }
}

.list-header {
  &__player {
    flex-grow: 1;
    flex-shrink: 1;
    padding-left: 32px;
    color: var(--color-text-gray);
  }

  &__level {
    width: 120px;
    color: var(--color-text-gray);
  }
}

@media (max-width: 575.98px) {
  .leaderboard-list {
    &__header {
      display: none;
    }
  }
}
